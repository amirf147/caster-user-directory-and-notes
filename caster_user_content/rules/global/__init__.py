# from dragonfly import List
# from caster_user_content.environment_variables import WINDOW_ALIASES

# # Create a Dragonfly List for aliases that will be shared across all rules in this package
# window_aliases = List("window_alias")
# window_aliases.set(WINDOW_ALIASES)

# # This makes the window_aliases available when importing from the package
# __all__ = ['window_aliases']

# This solution did not work for the error:
# Grammar Manager: Cannot add list while loaded.